{
  "name": "antd-less-to-css-variable",
  "version": "1.0.2",
  "description": "antd vue的less变量转成css variable",
  "main": "./lib/index.js",
  "scripts": {
    "build": "vite build && npm run minify",
    "minify": "esbuild --minify ./lib/index.js --outfile=dist/index.min.js --bundle --format=esm",
    "test": "node ./test/index"
  },
  "author": "xlzy520",
  "license": "MIT",
  "devDependencies": {
    "vite": "^2.9.1"
  }
}
